@switch (QuizUiOperation.Status)
{
    case UiOperationStatus.Uninitialised:
    case UiOperationStatus.Loading:
        <Blazorise.Text Color="TextColor.White">
            Loading...
        </Blazorise.Text>
        break;
    case UiOperationStatus.NotFound:
        <Blazorise.Text Color="TextColor.White">
            Not found
        </Blazorise.Text>
        break;
    case UiOperationStatus.Loaded:
        var quiz = QuizUiOperation.Result;
        <CascadingValue Value="quiz">
        <CascadingValue Value="quiz.Participants.FirstOrDefault(p => p.UserId == UserId)">
            @if (quiz.PublishedAt != null)
            {
                if (quiz.OwnerId == UserId)
                {
                    <HostQuiz />
                }
                else
                {
                    <TakeQuiz />
                }
            }
            else
            {
                if (quiz.OwnerId == UserId)
                {
                    <EditQuiz />
                }
                else
                {
                    <Blazorise.Text Color="TextColor.White">
                        Not found
                    </Blazorise.Text>
                }
            }
        </CascadingValue>
        </CascadingValue>
        break;
}